package com.alexzh.imbarista.ui.home

import org.junit.Assert.fail
import org.junit.Test
import org.koin.test.KoinTest

class HomeActivityTest : KoinTest {

    /**
     * - BottomNavigationBar should have 3 items: Coffee Drinks, Near Me, Profile
     */
    @Test
    fun shouldBeDisplayedBottomNavigationWith3Tabs() {
        fail()
    }

    /**
     * - Toolbar should have a text "Coffee Drinks" for the "Coffee Drinks" selected tab
     * - Toolbar should have a text "Cafes near Me" for the "Near Me" selected tab
     * - Toolbar should have a text "Profile" for the "Profile" selected tab
     */
    @Test
    fun shouldBeDisplayedCorrectToolbarTitleForEachTab() {
        fail()
    }
}